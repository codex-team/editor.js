/**
 * Fired when document selection is changed
 */
export const SelectionChanged = 'selection changed';

/**
 * Payload that will be passed with the event
 */
export interface SelectionChangedPayload {
}
